;(function($) {
	// Handlebars
	var source   = $('#twitter-template').html(),
		template = Handlebars.compile(source),
		$feed    = $('.twitter-feed');

	$(document).ready(function() {
		// Code goes here
	});
})(jQuery);